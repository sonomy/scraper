version: '3'

services:
  go:
    build:
      context: docker/.
    environment:
      - "SHELL=/bin/bash"
      - "GO11MODULE=on"
    volumes:
      - .:/app
      - /go
    ports:
      - 8080:8080
      - 8000:8000
    working_dir: /app
    #command: ./run
    command: sleep infinity
